let WEB_PORT = "3000";

let MONGODB_IP = "localhost";
let MONGODB_PORT = "27017";
let MONGODB_DATABASE_NAME = "app-hub";

let GITLAB_IP = "http://localhost/api/v4/";
let GITLAB_PRIVATE_TOKEN = "YOUR_PRIVATE_GITLAB_TOKEN";

let ANDROID_GROUP = "groups/8";
let IOS_GROUP = "groups/9";

module.exports = {
    WEB_PORT,
    MONGODB_IP,
    MONGODB_PORT,
    MONGODB_DATABASE_NAME,
    GITLAB_IP,
    GITLAB_PRIVATE_TOKEN,
    ANDROID_GROUP,
    IOS_GROUP
};
